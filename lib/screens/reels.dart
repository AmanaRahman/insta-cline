import 'package:flutter/material.dart';

class ReelsScreen extends StatelessWidget {
  final Key? key;

  const ReelsScreen({this.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('hello'),
    );
  }
}
